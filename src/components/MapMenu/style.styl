.map-menu {
  display: flex;
  position: absolute;
  top: 0;
  left: 50%;
  transform: translate(-50%);
  opacity: 0.8;

  .btn {
    &:first-child {
      border-bottom-left-radius: 20px;
    }

    &:last-child {
      border-bottom-right-radius: 20px;
    }
  }
}
